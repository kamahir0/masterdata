[project]
id = "fixture.full"
name = "Full Reference Fixture"
version = "0.1.0"

[sources]
roots = ["sources"]

[build]
output = ".masterdata/generated"
binary_output = ".masterdata/output/masterdata.bytes"
cache = ".masterdata/cache"

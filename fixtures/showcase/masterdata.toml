[project]
id = "fixture.showcase"
name = "Masterdata Showcase"
version = "0.1.0"

[sources]
roots = ["sources"]

[build]
artifact_dir = ".masterdata/output"
cache = ".masterdata/cache"

[build.profiles.production]
include_tags = ["production"]

[build.profiles.development]
include_tags = ["development"]
